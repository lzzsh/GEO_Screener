# 🎯 注解模式配置说明

## 你现在拥有什么

你的原始标准已经被转换成了一个**可重用的模板系统**。现在你可以：

### ✨ 核心功能

1. **默认模板** - 包含你原来的所有标注维度
   - 7 个 GSE 标签（数据集级别）
   - 18 个 GSM 标签（样本级别）

2. **模式管理** - 在 UI 中创建和管理多个模式
   - 基于默认模板快速创建
   - 自定义修改标签
   - 保存为可重用模式

3. **任务集成** - 创建任务时选择模式
   - 下拉菜单选择
   - 自动应用标签定义
   - LLM 自动提取

## 🚀 快速配置（3 步）

### 第 1 步：打开模式管理
```
1. 登录系统
2. 点击顶部 "Criteria" 链接
3. 切换到 "Annotation Schemas" 标签页
```

### 第 2 步：创建你的第一个模式
```
1. 点击 "📋 Use Default Template" 按钮
2. 修改名称（例如："我的 PSC 分化研究"）
3. 点击 "Save" 保存
```

### 第 3 步：在任务中使用
```
1. 创建新任务 → "New Screening Task"
2. 选择 "GEO Search" 标签页
3. 在 "Annotation Schema" 下拉菜单选择你的模式
4. 创建任务
```

完成！LLM 会自动使用你的标签定义。

## 📋 默认模式包含的标签

### GSE 标签（数据集级别）
你原来的标注维度已转换为：

```
✓ 数据模态 - 实际观察到的数据模态
  可选值：scRNA-seq, scATAC-seq, spatial transcriptomics, CITE-seq, multiome, bulk RNA-seq, ribosome profiling

✓ 分化起点 - 起始细胞类型
  可选值：iPSC, ESC, PSC

✓ 扰动类型 - 实验扰动类型
  可选值：TF, 小分子, CRISPR, 其他

✓ 分化体系 - 分化培养体系
  可选值：2D, 3D

✓ 分化终点 - 分化目标细胞类型
  自由文本输入

✓ 数据平台 - 测序平台
  自由文本输入

✓ 是否提供原始测序数据 - 原始数据可用性
  可选值：是, 否, 不明确
```

### GSM 标签（样本级别）
包含 18 个详细的样本级标签，用于提取每个样本的信息。

## 🎨 自定义你的模式

### 场景 1：针对特定研究类型
```
1. 使用默认模板
2. 修改名称为："心脏分化研究"
3. 修改 "分化终点" 的描述为："心脏细胞类型（心肌细胞、心房细胞等）"
4. 保存
```

### 场景 2：添加新标签
```
1. 使用默认模板
2. 在 GSE 标签中点击 "+ Add Label"
3. 填写：
   - 名称：custom_field
   - 描述：你的描述
   - 类型：free_text 或 enum
4. 保存
```

### 场景 3：创建简化版本
```
1. 点击 "+ New Schema"
2. 只添加你需要的标签
3. 保存为简化版本
```

## 💾 模式版本管理

### 推荐做法
```
版本 1.0 - 初始版本
  ↓
版本 1.1 - 添加新标签
  ↓
版本 2.0 - 重大改进
```

**不要修改旧版本**，创建新版本：
- 保持数据一致性
- 便于追踪变更
- 支持回滚

## 🔍 查看模式详情

### 在模式列表中
```
左侧面板显示：
- 模式名称
- 最后修改日期
- 点击选择查看详情
```

### 在编辑器中
```
右侧面板显示：
- 模式名称和描述
- GSE 标签列表（可编辑）
- GSM 标签列表（可编辑）
- 保存和删除按钮
```

## 📊 标签类型说明

### enum（枚举）- 预定义选项
```
示例：["是", "否", "不明确"]
用途：有限的选择项
LLM 行为：从列表中选择
```

### free_text（自由文本）- 开放输入
```
示例：细胞系名称、年龄
用途：任意文本输入
LLM 行为：生成相关文本
```

### array（数组）- 多个值
```
示例：["time_point_1", "time_point_2"]
用途：列表数据
LLM 行为：生成 JSON 数组
```

### object_array（对象数组）- 复杂结构
```
示例：[{"type": "CRISPR", "method": "..."}]
用途：结构化数据
LLM 行为：生成对象数组
```

## ⚙️ 配置建议

### 1. 开始使用
- ✅ 使用默认模板
- ✅ 在小规模任务中测试
- ✅ 检查 LLM 提取的质量

### 2. 优化标签
- ✅ 根据测试结果调整
- ✅ 添加缺失的标签
- ✅ 改进标签描述

### 3. 创建多个版本
- ✅ 为不同研究类型创建模式
- ✅ 保留版本历史
- ✅ 记录变更原因

### 4. 团队协作
- ✅ 共享模式名称约定
- ✅ 文档化标签含义
- ✅ 定期审查和更新

## 🎯 常见配置场景

### 场景 A：快速开始
```
1. 点击 "📋 Use Default Template"
2. 改名为 "我的研究"
3. 保存
4. 在任务中选择使用
```
⏱️ 耗时：2 分钟

### 场景 B：针对特定研究
```
1. 使用默认模板
2. 修改相关标签的描述
3. 添加 1-2 个自定义标签
4. 保存为新版本
```
⏱️ 耗时：10 分钟

### 场景 C：完全自定义
```
1. 点击 "+ New Schema"
2. 从零开始添加标签
3. 定义所有标签类型和值
4. 保存
```
⏱️ 耗时：30 分钟

## 📞 需要帮助？

### 快速参考
- 查看 `ANNOTATION_SCHEMA_QUICK_REF.md`
- 包含所有常见操作

### 详细指南
- 查看 `ANNOTATION_SCHEMA_GUIDE.md`
- 包含完整的配置说明

### 常见问题
```
Q: 如何修改已使用的模式？
A: 创建新版本，不要修改旧版本

Q: 删除模式会影响任务吗？
A: 不会，任务保存了模式快照

Q: 可以导出模式吗？
A: 目前需要手动复制，未来版本支持

Q: 标签顺序重要吗？
A: 是的，会影响 LLM 提示词
```

## ✅ 配置检查清单

- [ ] 已登录系统
- [ ] 已访问 Annotation Schemas 页面
- [ ] 已使用默认模板创建第一个模式
- [ ] 已修改模式名称
- [ ] 已保存模式
- [ ] 已在任务中选择模式
- [ ] 已创建测试任务
- [ ] 已检查 LLM 提取的标签

## 🎉 完成！

现在你可以：
- ✅ 使用默认模式快速开始
- ✅ 创建自定义模式满足需求
- ✅ 在任务中灵活选择模式
- ✅ 自动提取结构化元数据

祝你使用愉快！
